import type { Metadata } from 'next';
import SkillsContent from '@/components/SkillsContent';

export const metadata: Metadata = { title: 'Skills — Walid Chennit' };

export default function SkillsPage() {
  return <SkillsContent />;
}
